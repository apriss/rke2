apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring
spec:
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 3000
  type: LoadBalancer
  loadBalancerIP: 10.1.1.160
